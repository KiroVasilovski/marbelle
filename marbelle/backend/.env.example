# Production settings
ALLOWED_HOSTS=
CORS_ALLOWED_ORIGINS=
FRONTEND_URL=

# Django settings
SECRET_KEY=
DJANGO_SETTINGS_MODULE=

# Database settings
DB_NAME=
DB_USER=
DB_PASSWORD=
DB_HOST=
DB_PORT=

# Email settings
EMAIL_PORT=
EMAIL_USE_TLS=
EMAIL_USE_SSL=
EMAIL_HOST=
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=
DEFAULT_FROM_EMAIL=

# Session settings (optional - defaults provided in settings)
# For cross-subdomain cookie sharing, set to .yourdomain.com (e.g., .onrender.com)
SESSION_COOKIE_DOMAIN=

# Enable cross-site cookies for different subdomains (Safari support)
# Set to "true" when frontend and backend are on different subdomains
# Requires HTTPS (SESSION_COOKIE_SECURE=True in production)
ENABLE_CROSS_SITE_COOKIES=

# Cloudinary Configuration
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=